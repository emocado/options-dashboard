# Copy to .streamlit/secrets.toml (gitignored) — do NOT commit the real file.
# Generate the value with:  python tools/set_password.py
# (which writes APP_PASSWORD_HASH for you; you normally don't edit this by hand)

APP_PASSWORD_HASH = "pbkdf2_sha256$240000$<salt_hex>$<hash_hex>"
