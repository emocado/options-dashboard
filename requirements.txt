# Options Wheel Dashboard — base dependencies.
# This is what Streamlit Community Cloud installs, so keep it lean (no moomoo SDK).
streamlit>=1.42        # >=1.42 for native OIDC auth (st.login / st.user)
pandas>=2.0
plotly>=5.20

# Required by Streamlit's native Google sign-in (st.login). Authlib's Starlette
# OAuth client imports httpx, which Authlib doesn't install automatically — so
# list httpx explicitly or the /auth/login endpoint 500s with a misleading
# "requires Authlib" error.
Authlib>=1.3.2
httpx

# Remote DB backend (Turso / libSQL), imported as `libsql`.
# Used when TURSO_DATABASE_URL is set; otherwise the app falls back to local SQLite.
libsql

# Testing
pytest>=8.0
